import styled from "styled-components";

export const Checker = styled.div`
  border: white 2px solid;
  padding: 8px 16px;
  border-radius: 12px;
  position: relative;
  margin-top: 16px;

  user-select: none;

  :hover {
    cursor: pointer;
  }
`;
